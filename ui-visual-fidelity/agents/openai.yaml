interface:
  display_name: "UI Visual Fidelity"
  short_description: "Fix messy AI-generated UI without visual regressions"
  default_prompt: "Use $ui-visual-fidelity to clean up this AI-generated UI without changing its structure or behavior."
policy:
  allow_implicit_invocation: true
